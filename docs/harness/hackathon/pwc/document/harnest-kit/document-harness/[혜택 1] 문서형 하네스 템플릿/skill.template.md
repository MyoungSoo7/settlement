---
name: [skill-name]                       # kebab-case, 슬래시 명령으로 부를 이름
description: |
  [한 문장으로 이 스킬이 무엇을 자동화하는지. 무엇을 입력으로 무엇을 만드는지.]
  Triggers (KO): [한국어 트리거 단어/문구 3~5개, 쉼표로 구분]
  Triggers (EN): [영어 트리거 단어/문구 3~5개, 쉼표로 구분]
  Do NOT use when: [이 스킬을 쓰면 안 되는 경우 1~2개. 인접 스킬과의 경계.]
---

# [Skill Name]

## 역할

[이 스킬이 어떤 입장에서 일하는지 한 문단. "당신은 X 역할을 한다"가 아니라 "이 스킬은 X를 담당한다" 형태로.]

## 입력

[입력의 형식. 한 줄 요청인지, 구조화된 YAML인지, 파일 경로인지.]

## 출력

```[형식: yaml | markdown | json | text]
[출력 스키마. 슬롯 이름과 타입을 명시. 빈 슬롯 표기 규칙(empty, ___ 등) 포함.]
```

[출력 형식의 불변 조건 — 예: "키 이름은 항상 영문 snake_case", "list는 항상 1개 이상", "boundary는 한 문장."]

## 원칙

1. [원칙 한 줄. 무엇을 *항상* 한다.]
2. [원칙 한 줄.]
3. [원칙 한 줄.]

## 금지사항

- [무엇을 *절대* 하지 않는다 한 줄.]
- [무엇을 *절대* 하지 않는다 한 줄.]
- [인접 스킬 영역에 침범하지 않는 규칙.]

## 예시

### 입력

```
[현실적인 입력 한 건. 너무 짧지도 너무 길지도 않게.]
```

### 출력

```[형식]
[그 입력에 대해 이 스킬이 만들어내야 하는 출력 한 건. 출력 스키마와 정확히 일치해야 한다.]
```

## 인접 스킬과의 관계

- vs `[다른 스킬]`: [경계 한 줄. 어디까지 이 스킬, 어디부터 다른 스킬.]
- 이 스킬의 출력이 [다음 스킬]의 입력이 된다 / 이 스킬은 [상위 스킬]이 호출한다.

## 자가 검증

같은 입력을 두 번 넣어 같은 형식의 출력이 나오는지 확인한다. 형식이 같지 않다면 위 *출력* 절의 스키마를 더 좁힌다.
