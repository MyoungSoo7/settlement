# 2001-4-3. 하네스 선택표 완성하기

## Practice Task and Prompt

- Task: create `하네스 선택표` for clip `2001-4-3` so the learner can apply the curriculum message `md형 / 분업형 / MCP형 중 하나를 고른다` to their own work.
- Prompt to paste into AI:

```text
Fast Campus 하네스 엔지니어링 실습 2001-4-3을 도와줘.

Curriculum mapping:
- Module: 1-4. 하네스 선택 기준
- Clip: [실습] 하네스 선택표 완성하기
- Key message: md형 / 분업형 / MCP형 중 하나를 고른다
- Required output: 하네스 선택표

Interview mapping:
- Use the interview framing of layered harness choices: ambiguity removal, deterministic execution, and observability should be added only when the workflow actually needs them.

Task:
내 입력을 바탕으로 `하네스 선택표` 초안을 작성해줘. 아래 실습자료의 작성 템플릿 구조를 따르고, 모르는 정보는 추측하지 말고 "다시 물을 질문" 또는 "확인 필요"로 남겨.

Boundaries:
- 실습 자료 및 입력한 문서의 범위를 벗어난 외부 사례를 추가하지 않는다.
- 검증되지 않은 주장을 넣지 않는다.
- 최종 책임 판단이 필요한 항목은 사람이 결정할 수 있게 분리한다.

My input:
[여기에 내 업무 상황, 원문, 제약, 기존 산출물을 붙인다]
```

## 목적

내 업무에 필요한 하네스 유형을 md형, 분업형, MCP형 중에서 고른다. 규칙 고정, 역할 분리, 순서 강제 중 무엇이 핵심 문제인지 판단하고 다음에 만들 산출물을 정한다.

## Learner Objective

수강생은 하나의 실제 업무를 기준으로 `md형`, `분업형`, `MCP형` 중 가장 작은 충분한 하네스를 선택할 수 있어야 한다. 선택 결과에는 선택한 유형, 선택 이유, 다음에 만들 산출물, 지금은 하지 않을 일이 함께 남아야 한다.

## 권장 시간

20분

## 준비물

- 앞 실습에서 다룬 실패 사례, md 규칙서 초안, 세션 역할 분리표 중 1개 이상
- 선택하려는 실제 업무 1개
- 현재 업무에서 자주 무너지는 지점 목록

## Exercise Prompt

앞 실습에서 다룬 업무 하나를 가져와 하네스 선택표를 완성한다. 문서 규칙만으로 충분한지, 메인/보조 세션 역할 분리가 필요한지, 순서·상태·재시도를 코드나 도구로 강제해야 하는지 차례로 판단한다.

## Starter Context

아래 질문에 짧게 답한 뒤 선택표를 작성한다.

- 이 업무는 매번 같은 역할, 원칙, 금지사항, 출력 형식을 반복해서 설명하는가?
- 탐색, 초안, 검수, 판단이 한 세션에 몰려 메인 맥락이 흔들리는가?
- 순서, 상태, 재시도, 체크포인트를 사람이 매번 기억하기 어려운가?
- 지금 당장 필요한 산출물은 규칙서, 역할 분리표, MCP 설계 초안 중 무엇에 가까운가?
- 가장 작은 충분한 하네스로 시작하려면 무엇을 아직 하지 않아도 되는가?

## Expected Result

완성 산출물은 `md형`, `분업형`, `MCP형` 중 하나를 고른 하네스 선택표다. 표만 읽어도 왜 그 유형을 골랐는지, 다음에 만들 산출물이 무엇인지, 과한 하네스 설계를 피하기 위해 지금은 하지 않을 일이 무엇인지 보여야 한다.

## 입력

아래 입력을 먼저 적는다.

| 항목 | 내용 |
|---|---|
| 업무 이름 |  |
| 반복 규칙이 필요한가 |  |
| 역할 분리가 필요한가 |  |
| 순서, 상태, 재시도 강제가 필요한가 |  |
| 현재 가장 큰 실패 원인 |  |
| 다음에 만들 산출물 |  |

## 단계별 진행

1. 앞 실습에서 다룬 업무 중 하나를 고르고 업무 목표를 한 문장으로 적는다.
2. 현재 가장 큰 실패 원인을 `규칙 반복`, `메인 맥락 소모`, `순서·상태 관리 부담` 중 어디에 가까운지 표시한다.
3. 반복 규칙, 역할, 금지사항, 출력 형식만 고정하면 충분한지 확인한다.
4. 탐색, 초안, 독립 검수를 분리해 메인 세션의 맥락을 보호해야 하는지 확인한다.
5. 순서, 상태, 재시도, 체크포인트를 사람이 매번 기억하기 어려운지 확인한다.
6. `md형`, `분업형`, `MCP형` 중 가장 작은 충분한 유형을 하나 고른다.
7. 선택 이유를 규칙 고정, 역할 분리, 순서 강제 중 하나 이상의 기준으로 쓴다.
8. 다음에 만들 산출물과 지금은 하지 않을 일을 각각 한 줄로 정한다.

## 작성 템플릿

```markdown
# 하네스 선택표

## 업무
- 업무 이름:
- 업무 목표:
- 현재 가장 큰 실패 원인:

## 선택 기준
| 질문 | 예/아니오 | 근거 |
|---|---|---|
| 반복 규칙, 역할, 금지사항, 출력 형식만 고정하면 충분한가 |  |  |
| 탐색, 초안, 독립 검수를 분리해야 하는가 |  |  |
| 순서, 상태, 재시도, 체크포인트를 강제해야 하는가 |  |  |

## 선택
- 선택한 유형: md형 / 분업형 / MCP형
- 선택 이유:
- 다음에 만들 산출물:
- 지금은 하지 않을 것:
```

## 예시

```markdown
# 하네스 선택표

## 업무
- 업무 이름: 반복 문서 초안 작성
- 업무 목표: 입력 메모를 정해진 구조의 초안으로 바꾸기
- 현재 가장 큰 실패 원인: 매번 역할과 금지사항을 다시 설명한다.

## 선택 기준
| 질문 | 예/아니오 | 근거 |
|---|---|---|
| 반복 규칙, 역할, 금지사항, 출력 형식만 고정하면 충분한가 | 예 | 같은 규칙을 매번 설명하는 문제가 가장 크다. |
| 탐색, 초안, 독립 검수를 분리해야 하는가 | 아니오 | 아직 업무 규모가 작고 메인 맥락이 크게 소모되지 않는다. |
| 순서, 상태, 재시도, 체크포인트를 강제해야 하는가 | 아니오 | 사람이 순서를 확인해도 부담이 크지 않다. |

## 선택
- 선택한 유형: md형
- 선택 이유: 반복 규칙과 금지사항을 문서로 고정하는 것이 먼저다.
- 다음에 만들 산출물: md 규칙서 초안
- 지금은 하지 않을 것: 순서와 재시도를 코드로 강제하는 MCP형 설계
```

## Completion Checks

- 선택한 유형이 현재 실패 원인과 직접 연결된다.
- md형은 반복 규칙 고정, 분업형은 메인 세션 보호, MCP형은 순서·상태·재시도 강제 기준으로 판단했다.
- 다음에 만들 산출물이 선택한 유형과 맞는다.
- 지금은 하지 않을 일을 적어 과한 하네스 설계를 피했다.

## 완료 기준

- md형, 분업형, MCP형 중 하나가 선택되어 있다.
- 선택 이유가 규칙 고정, 역할 분리, 순서 강제 중 하나 이상의 기준으로 설명되어 있다.
- 다음에 만들 산출물이 한 줄로 적혀 있다.
- 지금은 하지 않을 것이 함께 적혀 있어 과한 하네스 설계를 피한다.

## 제출/검토 체크리스트

- [ ] 선택한 유형과 선택 이유가 서로 맞는다.
- [ ] md형은 반복 규칙 고정, 분업형은 메인 세션 보호, MCP형은 순서·상태·재시도 강제 기준으로 판단했다.
- [ ] MCP를 모든 업무의 기본값으로 두지 않았다.
- [ ] 다음 실천 산출물이 명확하다.
- [ ] 새로운 외부 사례, 검증되지 않은 수치, 가격 또는 마케팅성 정보를 넣지 않았다.
