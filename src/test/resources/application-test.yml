spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;INIT=CREATE SCHEMA IF NOT EXISTS opslab
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: false
  batch:
    jdbc:
      initialize-schema: always
  elasticsearch:
    enabled: false
  data:
    elasticsearch:
      repositories:
        enabled: false

toss:
  secret-key: test_sk_12345
  api-url: http://localhost:8080/mock/toss

app:
  jwt:
    issuer: lemuel-test
    secret: test-secret-key-test-secret-key-test-secret-key-test-secret-key
    ttl-seconds: 3600
  password-reset:
    token-expiry-minutes: 30
    base-url: http://localhost:5173
