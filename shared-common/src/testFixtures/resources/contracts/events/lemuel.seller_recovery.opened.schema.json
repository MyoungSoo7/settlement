{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "lemuel.seller_recovery.opened",
  "description": "지급 완료 후 회수가 필요한 채권(SellerRecovery)이 발생한 사실. amount 는 JSON string(BigDecimal.toPlainString, DATA-STANDARD N5).",
  "$comment": "참고(계약 아님) — 회계 해석: DR SELLER_RECOVERY_RECEIVABLE / CR CASH(지급후 회수채권 발생, ADR 0026 Option ①).",
  "type": "object",
  "properties": {
    "recoveryId": { "type": "integer" },
    "sellerId": { "type": "integer" },
    "amount": { "type": "string", "pattern": "^[0-9]+(\\.[0-9]+)?$" }
  },
  "required": ["recoveryId", "sellerId", "amount"],
  "additionalProperties": true
}
