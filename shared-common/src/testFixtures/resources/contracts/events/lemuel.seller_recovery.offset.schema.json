{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "lemuel.seller_recovery.offset",
  "description": "회수채권(SellerRecovery)이 이후 정산금과 상계된 사실. amount 는 JSON string(BigDecimal.toPlainString, DATA-STANDARD N5).",
  "$comment": "참고(계약 아님) — 회계 해석: DR SELLER_PAYABLE / CR SELLER_RECOVERY_RECEIVABLE(회수 상계, ADR 0026 Option ①).",
  "type": "object",
  "properties": {
    "allocationId": { "type": "integer" },
    "recoveryId": { "type": "integer" },
    "sellerId": { "type": "integer" },
    "amount": { "type": "string", "pattern": "^[0-9]+(\\.[0-9]+)?$" }
  },
  "required": ["allocationId", "sellerId", "amount"],
  "additionalProperties": true
}
