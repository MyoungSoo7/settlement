{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "lemuel.seller_recovery.offset",
  "description": "settlement-service → account-service. 회수채권(SellerRecovery)이 이후 정산금과 상계될 때 발행되어 GL 에 DR SELLER_PAYABLE / CR SELLER_RECOVERY_RECEIVABLE(회수 상계)로 전기된다(ADR 0026 Option ①). amount 는 JSON number.",
  "type": "object",
  "properties": {
    "allocationId": { "type": "integer" },
    "recoveryId": { "type": "integer" },
    "sellerId": { "type": "integer" },
    "amount": { "type": "number", "minimum": 0 }
  },
  "required": ["allocationId", "sellerId", "amount"],
  "additionalProperties": true
}
