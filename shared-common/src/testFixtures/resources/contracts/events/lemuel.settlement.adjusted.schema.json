{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "lemuel.settlement.adjusted",
  "description": "확정 전 정산금이 조정된 사실(오류 정정·차감 등). targetLeg 는 조정이 걸린 몫을 가리킨다 — 즉시분이면 SELLER_PAYABLE, 유보분이면 HOLDBACK_PAYABLE. 금액(amount)은 JSON string(BigDecimal.toPlainString, DATA-STANDARD N5).",
  "$comment": "참고(계약 아님) — 회계 해석: DR {targetLeg} / CR CASH(ADR 0026 Option ①).",
  "type": "object",
  "properties": {
    "adjustmentId": { "type": "integer" },
    "settlementId": { "type": "integer" },
    "sellerId": { "type": "integer" },
    "amount": { "type": "string", "pattern": "^[0-9]+(\\.[0-9]+)?$" },
    "targetLeg": { "type": "string", "enum": ["SELLER_PAYABLE", "HOLDBACK_PAYABLE"] }
  },
  "required": ["adjustmentId", "sellerId", "amount", "targetLeg"],
  "additionalProperties": true
}
