{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "lemuel.loan.repayment_applied",
  "description": "정산건에서 대출 상환액이 차감 적용된 사실. 정산건별 차감액(deducted)을 보존한다. 차감 0(대출 없는 셀러)도 정상 발행되므로 minimum 0. deducted 는 JSON string(BigDecimal.toPlainString, DATA-STANDARD N5).",
  "type": "object",
  "properties": {
    "settlementId": { "type": "integer" },
    "sellerId": { "type": "integer" },
    "deducted": { "type": "string", "pattern": "^[0-9]+(\\.[0-9]+)?$" }
  },
  "required": ["settlementId", "sellerId", "deducted"],
  "additionalProperties": true
}
