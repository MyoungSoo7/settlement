{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "lemuel.payout.completed",
  "description": "셀러 정산금이 실지급(payout)된 사실. 금액(amount)은 JSON string(BigDecimal.toPlainString, DATA-STANDARD N5). settlementId 는 수동 송금 시 null 허용.",
  "$comment": "참고(계약 아님) — 회계 해석: DR SELLER_PAYABLE / CR CASH(미지급금 상계 + 현금 유출, ADR 0026 Option A).",
  "type": "object",
  "properties": {
    "payoutId": { "type": "integer" },
    "settlementId": { "type": ["integer", "null"] },
    "sellerId": { "type": "integer" },
    "amount": { "type": "string", "pattern": "^[0-9]+(\\.[0-9]+)?$" }
  },
  "required": ["payoutId", "sellerId", "amount"],
  "additionalProperties": true
}
