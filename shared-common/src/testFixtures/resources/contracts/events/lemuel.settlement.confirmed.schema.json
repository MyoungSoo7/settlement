{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "lemuel.settlement.confirmed",
  "description": "settlement-service(SettlementKafkaEventPublisherAdapter) → loan-service(SettlementConfirmedConsumer). 상환 saga 의 loan 측 진입점 — 확정 정산금(amount)에서 미상환 대출을 FIFO 차감한다. amount 는 JSON number(settlement 계열 직렬화 규약).",
  "type": "object",
  "properties": {
    "settlementId": { "type": "integer" },
    "sellerId": { "type": "integer" },
    "amount": { "type": "number" }
  },
  "required": ["settlementId", "sellerId", "amount"],
  "additionalProperties": true
}
