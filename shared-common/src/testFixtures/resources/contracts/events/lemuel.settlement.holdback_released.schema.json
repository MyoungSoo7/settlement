{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "lemuel.settlement.holdback_released",
  "description": "홀드백 보유기간이 만료되어 유보금이 지급 가능 상태로 재분류된 사실. 금액(amount)은 JSON string(BigDecimal.toPlainString, DATA-STANDARD N5).",
  "$comment": "참고(계약 아님) — 회계 해석: DR HOLDBACK_PAYABLE / CR SELLER_PAYABLE(유보 해제 재분류, ADR 0026 Option ①).",
  "type": "object",
  "properties": {
    "settlementId": { "type": "integer" },
    "sellerId": { "type": "integer" },
    "amount": { "type": "string", "pattern": "^[0-9]+(\\.[0-9]+)?$" }
  },
  "required": ["settlementId", "sellerId", "amount"],
  "additionalProperties": true
}
