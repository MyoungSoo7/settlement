{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "lemuel.loan.corporate_loan_disbursed",
  "description": "상장사 기업 신용대출이 실행(지급)된 사실. principal/fee 는 금액이므로 JSON string(BigDecimal.toPlainString, DATA-STANDARD N5)(전 계열 통일). fee 는 0 가능(무수수료 조건)이므로 비음수 패턴. stockCode 는 6자리 종목코드.",
  "type": "object",
  "properties": {
    "loanId": { "type": "integer" },
    "stockCode": { "type": "string", "pattern": "^[0-9]{6}$" },
    "corpName": { "type": "string" },
    "principal": { "type": "string", "pattern": "^-?[0-9]+(\\.[0-9]+)?$" },
    "fee": { "type": "string", "pattern": "^[0-9]+(\\.[0-9]+)?$" }
  },
  "required": ["loanId", "stockCode", "corpName", "principal", "fee"],
  "additionalProperties": true
}
