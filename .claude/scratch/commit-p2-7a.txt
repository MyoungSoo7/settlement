feat(loan): 기준금리 economics 실연동 (설정값 폴백) — P2-7a

- EconomicsBaseRateAdapter: GET /api/economics/indicators/BASE_RATE/latest 의
  latest.value 를 기준금리로 쓴다(ECOS 722Y001). Phase 1 설정값 어댑터
  (ConfiguredBaseRateAdapter)를 교체하되 그 설정값은 폴백으로 남긴다.
  ★ 폴백 경로가 넓다 — ECOS 수집이 안 돈 환경은 latest=null 이 정상 응답이고,
    미기동이면 접속 자체가 실패한다. 어느 경우든 기준금리 조달 실패가 대출 신청을
    막으면 안 되므로(가용성 우선) 폴백하고 경고 로그만 남긴다. 조달된 금리는 신청
    시점 스냅샷으로 박혀 사후에 흔들리지 않는다.
  ★ 중도상환 경로는 이 포트를 타지 않는다(P2-6 에서 수수료를 정적 순수 함수로
    분리해 둔 결합 차단이 여기서 완성된다).
  포트(BaseRatePort)만 보는 도메인·응용 계층은 한 줄도 수정하지 않았다 — Phase 1
  이 "이 클래스만 교체된다"고 적어 둔 경계가 실제로 작동함을 확인.
- 배선: app.loan.economics.base-url (+ secured 정책 키 yml 표면화) · compose 에
  APP_LOAN_ECONOMICS_BASE_URL=http://economics-service:8080 (investment 선례 관례).
- SecuredLoanLifecycleIT: base-url 을 9번 포트(discard)로 고정 — 로컬에 economics
  가 떠 있으면 실측 금리가 섞여 IT 가 비결정적이 되는 것을 차단(폴백 3.5% 고정).

검증: :loan-service:test + jacocoTestCoverageVerification(LINE 90%) 통과.
EconomicsBaseRateAdapterTest 4/4(정상·latest null·404·5xx 폴백) ·
SecuredLoanLifecycleIT 9/9 (skipped=0).
TDD: 정상 경로 1건 RED 목격 후 GREEN(폴백 3경로는 스켈레톤에서 이미 통과).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
