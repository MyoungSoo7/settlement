feat(loan): 중도상환 + 실행시각 스냅샷 — P2-6

- SecuredLoan.prepay(): 정상(DISBURSED) 전용 중도상환. 연체·기한이익상실 중 납입은
  회차 상환(repay) 경로다 — 수수료가 붙는 중도상환으로 받으면 연체자에게 수수료까지
  물리게 되므로 상태머신이 막는다.
- SecuredLoan.disburse(LocalDateTime): 실행 시각 스냅샷(disbursedAt).
  ★ 수수료 약정기간(경과 3년 면제)의 기산점은 신청일이 아니라 실행일이다 — 승인이
    늦어진 대출이 신청일 기산으로 면제 기간을 앞당겨 받으면 안 된다. 컬럼 도입 이전
    실행분(NULL)은 createdAt 폴백 — 신청일이 실행일보다 이르므로 차주에게 불리해지지
    않는 방향의 근사다. 선정산 loans 의 disbursed_at 후행 추가와 같은 경로.
- SecuredLoanPolicy.earlyRepaymentFee static 전환.
  ★ 주입값(기준금리·LTV)과 무관한 상수 테이블 순수 함수라 정적으로 뒀다 — 중도상환
    경로가 기준금리 조달(Phase 2: economics-service 원격 호출)에 결합되면 기준금리
    장애가 중도상환을 막는 사고 경로가 생긴다.
- PrepaySecuredLoanService(신규): 비관적 락 → 소유권 403(IDOR 이중 방어) →
  SEC_REPAYMENT(차 CASH/대 LOAN_RECEIVABLE) + 수수료>0 시 SEC_EARLY_FEE(차 CASH/대
  FEE_INCOME) → 완제 시 담보 말소 + repaid 이벤트(이자 0).
  ★ 면제·0원 수수료는 기표하지 않는다 — 원장 전표 양수 금액 불변식. 테스트가 "수수료
    전표가 없음"까지 확인한다.
  ★ 수수료는 clamp 된 실제 차감액 기준 — 잔액 초과 요청에 과다 청구가 없다.
- 웹: POST /loans/secured/{loanId}/prepay — 응답에 차감액·수수료 명시(fee-on-top,
  총 수취액 = prepaidAmount + fee 로 계약 고정).
- 멱등: /prepay·/repay 에 Idempotency-Key 선점 배선 (gl-ledger-auditor 치명 지적 반영).
  ★ 비관락은 동시 요청 직렬화만 하고 커밋 후 순차 재제출은 못 막는데, SEC_REPAYMENT·
    SEC_EARLY_FEE 는 회차성이라 원장 유니크에서도 제외 — 재제출이 잔액 이중 차감·수수료
    이중 수취로 직행하고, 각 전표가 자기균형이라 시산표로도 영구히 안 잡힌다.
    CorporateLoanController 상환 #4 선례(LoanManualIdempotencyGuard)와 동형으로 봉합.
    /repay 의 같은 공백은 이번 변경 이전부터 있던 것을 함께 닫았다.
- V20260730220000: secured_loans.disbursed_at 추가 + CHECK(disbursed_at >= created_at).
  구 행은 NULL 유지(소급 복원 불가, NOT NULL 불가).

검증: :loan-service:test + jacocoTestCoverageVerification(LINE 90%) 통과.
SecuredLoanLifecycleIT 9/9 (skipped=0) — 중도상환 2회(부분→전액) 실 DB 완주,
SEC_EARLY_FEE N회 허용(P2-3 유니크 제외)을 실측으로 확인.
SecuredLoanControllerTest 5/5 — 중복 키 409·use case 미호출까지 관찰.
gl-ledger-auditor 감사: 치명 1건(멱등) 반영, 전표 방향·0원 미기표·clamp 기준
수수료·완제 연쇄 대칭은 이상 없음 판정. 권고(수수료 taper 분모·이벤트 수수료 필드·
FEE_INCOME 세분화 등)는 후속 이월.
TDD: 도메인 7건 RED → GREEN, 서비스 4건 RED → GREEN, 컨트롤러 2건 RED → GREEN.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
